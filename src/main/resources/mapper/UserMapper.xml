<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lynn.emergence.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.lynn.emergence.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="name" property="name"/>
        <result column="role" property="role"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="department" property="department"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="findByUsername" resultMap="BaseResultMap">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM user ORDER BY create_time DESC
    </select>

    <insert id="insert">
        INSERT INTO user (id, username, password, name, role, phone, email, department, status)
        VALUES (#{id}, #{username}, #{password}, #{name}, #{role}, #{phone}, #{email}, #{department}, #{status})
    </insert>

    <update id="update">
        UPDATE user
        SET username = #{username}, password = #{password}, name = #{name}, role = #{role},
            phone = #{phone}, email = #{email}, department = #{department}, status = #{status}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>
</mapper>

