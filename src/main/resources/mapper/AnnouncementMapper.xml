<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lynn.emergence.mapper.AnnouncementMapper">
    <resultMap id="BaseResultMap" type="com.lynn.emergence.entity.Announcement">
        <id column="id" property="id"/>
        <result column="event_id" property="eventId"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="type" property="type"/>
        <result column="target_audience" property="targetAudience"/>
        <result column="publisher_id" property="publisherId"/>
        <result column="publisher_name" property="publisherName"/>
        <result column="status" property="status"/>
        <result column="publish_time" property="publishTime"/>
        <result column="view_count" property="viewCount"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM announcement WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM announcement ORDER BY publish_time DESC
    </select>

    <select id="findByEventId" resultMap="BaseResultMap">
        SELECT * FROM announcement WHERE event_id = #{eventId} ORDER BY publish_time DESC
    </select>

    <select id="findByStatus" resultMap="BaseResultMap">
        SELECT * FROM announcement WHERE status = #{status} ORDER BY publish_time DESC
    </select>

    <select id="findByType" resultMap="BaseResultMap">
        SELECT * FROM announcement WHERE type = #{type} ORDER BY publish_time DESC
    </select>

    <insert id="insert">
        INSERT INTO announcement (id, event_id, title, content, type, target_audience, publisher_id, publisher_name, status, view_count)
        VALUES (#{id}, #{eventId}, #{title}, #{content}, #{type}, #{targetAudience}, #{publisherId}, #{publisherName}, #{status}, #{viewCount})
    </insert>

    <update id="update">
        UPDATE announcement
        SET event_id = #{eventId}, title = #{title}, content = #{content}, type = #{type},
            target_audience = #{targetAudience}, publisher_id = #{publisherId}, publisher_name = #{publisherName},
            status = #{status}, view_count = #{viewCount}
        WHERE id = #{id}
    </update>

    <update id="incrementViewCount">
        UPDATE announcement SET view_count = view_count + 1 WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM announcement WHERE id = #{id}
    </delete>
</mapper>

